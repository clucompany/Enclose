language: rust
cache: cargo
rust:
  - stable
  - beta
  - nightly
branches:
  only:
    - master
    - 1.1.8
matrix:
  allow_failures:
    - rust: nightly
script:
  - cargo build --verbose
  - cargo test --verbose

